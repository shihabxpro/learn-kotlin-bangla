# Kotlin Journey / Kotlin Kitty

Kotlin Fundamentals to Advanced concepts interactive learning web platform in Bengali.

## Run Locally

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
